```
Cấu trúc vòng for giống hàm khởi tạo:
    1. Khởi tạo những giá trị bất kỳ
    
    for (int i = 0; i < n; i++) {
        System.out.println(i);
    }
```

``` 
Getter/ Setter --> Đảm bảo tính vẹn toàn của dữ liệu --> Tính đóng gói
--> Một số dữ liệu cần được bảo mật và chỉ người có quyền được truy cập
--> Truy cập một cách gián tiếp vào dữ liệu
--> Getter/ Setter
```
